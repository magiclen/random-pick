[package]
name = "random-pick"
version = "1.2.3"
authors = ["Magic Len <len@magiclen.org>"]
repository = "https://github.com/magiclen/random-pick"
homepage = "https://magiclen.org/random-pick"
keywords = ["random", "pick", "choose", "array", "slice"]
categories = ["algorithms"]
description= "Pick an element from a slice randomly by given weights."
readme = "README.md"
license = "MIT"

[badges]
travis-ci = { repository = "magiclen/random-pick", branch = "master" }
appveyor = { repository = "magiclen/random-pick", branch = "master", service = "github" }

[dependencies]
random-integer = "1.0.0"